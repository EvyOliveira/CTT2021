# Lista de Exercícios III

> À partir do dia 23 de fevereiro. Aula de introdução ao JavaScript com foco em tipos de variáveis.  

```js

// Criar uma variável de escopo global ou função e que pode ter seu valor alterado.
?

// Item: Criar uma variável de escopo de bloco ou função e que NÃO pode ter seu valor alterado.
?

// Subitem: Tentar alterar o valor dessa variável e ver o que acontece.
?

// Criar uma variável de escopo de bloco ou função e que pode ter seu valor alterado.
?

// Item: Criar uma variável de escopo de bloco ou função e que NÃO pode ter seu valor alterado, inicializando a mesma com o valor = 10.
?

// Subitem: Tenta alterar esse valor para 5.
?

// Item: Criar uma variável de escopo global ou função e que pode ter seu valor alterado, inicializando a mesma com o valor = 10.
?

// Subitem: Tentar alterar esse valor para string ⇒ 'teste'.
?

// Item: Criar uma variável de escopo global ou função e que pode ter seu valor alterado. Inicializando a mesma com o valor undefined. 
?

// Subitem: Tentar alterar esse valor para null.
?

// Item: Criar uma variável de escopo de bloco ou função e que NÃO pode ter seu valor alterado, com o nome de objetoTeste, inicializando a mesma com o valor ⇒ {}. 
?

// Subitem 1: Tentar alterar esse valor para null.
?

// Subitem 2: Tentar incluir uma propriedade neste objeto da seguinte forma: objetoTeste.a = 10.
?

// Subitem 3: Tentar alterar o valor da propriedade a para = 5.
?

// Subitem 4: Tentar incluir uma propriedade nome neste objeto, inicializando com o valor 'teste'.
?

```
