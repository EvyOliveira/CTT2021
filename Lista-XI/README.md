# Lista de Exercícios XI

> À partir do dia 4 de março. Trabalhamos com live code manipulando arrays e suas operações.

#### Nível I
```js

// Iniciar um novo projeto com nome exercicios-de-array.
?

// Instalar o pacote nodemon.
?

// Criar o script "dev" para executar a rotina "nodemon index.js".
?

// Em index.js: criar uma função chamada "preencherArray()" que retorne a criação de um array com n de posições passadas no array.
?

// Listar a quantidade de posições no array.
?

// Criar uma função chamada "parOuImpar()" que passe a variável "tipo" e permita filtrar valores de um array entre par ou ímpar.
?

// Chamar a função par ou ímpar, e mostrar todos os valores em apenas uma linha.
?

// Criar um array chamado vemDeTudo. Crie uma função chamada "jogatudoPraDentro()" que permita armazenar qualquer tipo de lemento, objeto, número ou função dentro do array vemDeTudo();
?

```

#### Nível II
```js

// Item: Pegando o Array "vemDeTudo", adicionar posições nele no formato inteiro, string e funções. Criar uma nova função chamada processamento, onde:

/* > Se for número, deverá multiplicar os valores.
> Se for Array, armazená-lo em um array.
> Se for uma função, armazená-lo em outro array.
> O retorno desse processamento deve ser retornado em um objeto, como abaixo:
*/

{
  inteiros: 10,
  funcoes: [[Function], [Function]],
  strings: ["String1", "String2"]
}

```
