# Lista de Exercícios XII

> À partir do dia 12 de março. A aula referente à essa Lista de Exercícios foi sobre API Rest.  

```js

// O que é uma API?
[] Pequenos serviços independentes que se comunicam via HTTP.
[] Definições de um software para integração de suas funções por outra aplicação.
[] Aplicação dividida em camadas de acordo com suas responsabilidades.
[] Rotinas baseadas em cliente-servidor visando a obtenção de recursos.

// "Uma API que permite que sua solução ou serviço se comunique com outros serviços sem saber como estes foram implmentados".
[] Verdadeiro
[] Falso

// "Todas as APIs são Web Services, mas nem todos os Web Services são APIs".
[] Verdadeiro
[] Falso

// Qual é a diferença entre APIs e Web Services?
[] APIs devem respeitar o padrão REST | Web Services são flexíveis.
[] Web Services devem respeitar o padrão REST | APIs são flexíveis.
[] Web Services não necessariamente precisam se comunicar via Web | APIs sim.
[] APIs não necessariamente precisam se comunicar via Web | Web Services sim.

// "Nele podem estar contidos especificações das APIs, regras de acesso, boas práticas e outras informações necessárias".
[] API Plataform.
[] Dissertação de Roy Thomas Fielding, do ano 2000.
[] Portal do desenvolvedor.
[] RFCs.

// "Uma API que não se comunica utilizando o protocolo HTTP e dados no formato JSON pode ser RESTful?
[] Verdadeiro
[] Falso

// Uma API RESTful é uma API que...
[] Respeita os princípios fundamentais REST tendo sempre comunicação via web.
[] É baseada na comunicação HTTP e envio de dados no formato JSON.
[] É baseado somente em disponibilizar funções de um sistema como um serviço.
[] Respeita os princípios fundamentais REST tendo ou não comunicação via web.

// Qual dessas opções NÃO é um dos princípios fundamentais (constraints) do REST?
[] Segurança.
[] Cliente-servidor.
[] Cache.
[] Stateless.

// Pensando no princípio Client-Server do REST é papel do servidor: 
[] A interface de comunicação com o cliente.
[] A experiência do usuário do lado do cliente.
[] Gerenciamento de cache das chamadas enviadas pelo cliente.
[] Guarda estado do cliente.

// Sobre o princípio Stateless, é correto afirmar que:
[] O cliente pode obter a resposta correta mesmo enviando parte da informação.
[] Requisições são independentes, o servidor não guarda o estado do cliente.
[] Requisições são interdependentes, o servidor não guarda o estado do cliente.
[] Requisições são independentes, o servidor guarda o estado do cliente.

// O que é HTTP?
[] Protocolo de comunicação usado entre aplicações numa mesma máquina.
[] Protocolo de mensagens destinado a sensores e pequenos dispositivos móveis. 
[] Protocolo de comunicação para uso com nós restritos em redes da IoT.
[] Protocolo de comunicação cliente-servidor.

// Ao acessar um site utilizando um navegador, qual o método da requisição feita da máquina para os servidores da internet?
[] POST.
[] GET.
[] PUT.
[] PATCH.

// "Toda comunicação HTTP necessita de um body/payload. Essa afirmação é:
[] Verdadeiro
[] Falso

// Como nomeamaos a principal abstração de informações no REST?
[] API.
[] Elemento.
[] Recurso.
[] Classe.

// Qual o recurso (URN) no seguinte endereço web: "https://www.mycompany.com/career"?
[] career.
[] www.mycompany.com/career.
[] www.mycompany.com.
[] https://www.mycompany.com.

```
